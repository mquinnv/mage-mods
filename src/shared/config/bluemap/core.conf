# BlueMap core configuration
# This file configures the basic settings for BlueMap

# Accept file download for required resources
accept-download: true

# Enable web server for map viewing
webserver-enabled: true

# Web server port
webserver-port: 8100

# Data directory for maps
data: "bluemap"

# Map render threads (auto-detect based on CPU cores)
render-thread-count: 0